# scam-call-trainer

Read `docs/STARTER.md` first — it is the project brief: concept, verified data sources, safety guardrails, house rules, and definition of done.

Workflow: brainstorm + grill → written spec in `docs/` → TDD plan → implement. Do not start coding before a spec exists.

Key rules (details in STARTER):
- Safety first: inoculation training only, refuse real-scam-script generation, AOC 1441 referral always visible.
- AI = Gemini free tier behind a server-side proxy (Vercel), key never client-side.
- $0 runtime, official statistics only, cite on-page.
- UI: styled buttons, never naked links.
- Ship = armory repo updated (read armory projects.json by slug `scam-call-trainer`, not id).
